# DeepSeek API Key（必填，仅在服务端使用，不会暴露给浏览器）
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# 可选：模型名，默认 deepseek-chat
# DEEPSEEK_MODEL=deepseek-chat

# 可选：本地 SQLite 文件路径，默认 file:./speech.db
# DATABASE_FILE=file:./speech.db
